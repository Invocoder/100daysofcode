#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main(){
    int long i,j;
    int flag;
    int long low, high;
    cin>>low>>high;
  
    //while(low<high)
    for(i=low; i<=high; i++)
    {   flag=0;
        for(j=2; j<=sqrt(i); j++){
           if(i%j==0){
               flag=1;
               break;
           }
        }
        if(flag==0){
            cout<<i<<endl;
        }
        //low++;
        
    }
        
        
    
    return 0;
}

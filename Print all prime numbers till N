#include<iostream>
using namespace std;
int main()
{   
    int low,high,i,j,count;
    cin>>low;
    cin>>high;
    for(i=low;i<=high;i++)
    {   
        count=0;
        for(j=2;j*j<=i;j++)
        {
            if(i%j==0)
            {
                count=1;
            }
            
            
        }
        if(count==0)
        {
            cout<<i<<endl;
        }
    }
    return 0;
}
